<h1 class="text-3xl font-bold text-gray-800 mb-6"><%= pageTitle %></h1>

<div class="bg-white p-8 rounded-lg shadow-md">
    <form action="/admin/page/<%= pageKey %>" method="POST" class="space-y-6">
        <div>
            <label for="content" class="block text-sm font-medium text-gray-700">Nội dung trang</label>
            <textarea name="content" id="content" rows="20" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm focus:ring-indigo-500 focus:border-indigo-500"><%= content %></textarea>
            <p class="mt-2 text-sm text-gray-500">Bạn có thể sử dụng các thẻ HTML cơ bản để định dạng văn bản.</p>
        </div>

        <div class="flex justify-end space-x-4">
            <a href="/admin/settings" class="px-4 py-2 bg-gray-200 text-gray-800 rounded-md hover:bg-gray-300">Quay lại Cài đặt</a>
            <button type="submit" class="px-6 py-2 bg-indigo-600 text-white font-semibold rounded-md shadow-md hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">
                Lưu thay đổi
            </button>
        </div>
    </form>
</div>