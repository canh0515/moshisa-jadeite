<%- include('../layouts/admin-header') %>

<div class="container my-5">
    <h1>Sửa bài viết kiến thức</h1>
    <hr>
    <form action="/admin/knowledge/edit/<%= article.id %>" method="POST">
        <div class="mb-3">
            <label for="title" class="form-label">Tiêu đề bài viết</label>
            <input type="text" class="form-control" id="title" name="title" value="<%= article.title %>" required>
        </div>
        <div class="mb-3">
            <label for="category" class="form-label">Danh mục (Loại ngọc)</label>
            <select class="form-select" id="category" name="category" required>
                <% if (categories && categories.length > 0) { %>
                    <% categories.forEach(cat => { %>
                        <option value="<%= cat.name %>" <%= article.category === cat.name ? 'selected' : '' %>><%= cat.name %></option>
                    <% }) %>
                <% } %>
            </select>
            <div class="form-text">Chọn danh mục kiến thức. Danh mục này sẽ được dùng để liên kết với các sản phẩm tương ứng.</div>
        </div>
        <div class="mb-3">
            <label for="content" class="form-label">Nội dung bài viết</label>
            <textarea class="form-control" id="content" name="content" rows="10" required><%= article.content %></textarea>
            <div class="form-text">Bạn có thể sử dụng HTML cơ bản để định dạng bài viết.</div>
        </div>
        
        <button type="submit" class="btn btn-primary">Lưu thay đổi</button>
        <a href="/admin/knowledge" class="btn btn-secondary">Hủy</a>
    </form>
</div>

<%- include('../layouts/admin-footer') %>